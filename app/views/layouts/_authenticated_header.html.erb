

<div class="flex items-center">
  <div class="flex-shrink-0">
    <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: 'my-2 bg-gray-800 p-1 text-gray-400 rounded-lg bg-gray-200 text-black font-medium cursor-pointer block' %>
  </div>
  <div class="hidden md:ml-4 md:flex md:flex-shrink-0 md:items-center">
    <div class="flex-shrink-0">
      <p class="p-1">
        Hi, <%= "#{user.name} #{user.admin ? ' (Admin)' : ''}, #{user.email} " %>
      </p>
    </div>
  </div>
</div>
