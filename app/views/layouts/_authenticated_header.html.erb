<nav class="bg-gray-100 py-4">
  <div class="container mx-auto flex justify-between items-center">
    <ul class="flex">
      <li class="mr-4">
        <%= link_to 'Home', root_path, class: 'text-blue-500 hover:text-blue-700' %>
      </li>
    </ul>
    <div class="text-center">
      Hi, <%= "#{user.name} #{user.admin ? ' (Admin)' : ''}" %><br>
      <%= user.email %><br>
    </div>
    <div>
      <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: 'ml-2 rounded-lg py-3 px-5 inline-block font-medium bg-red-200 text-red-500 hover:text-red-700' %>
    </div>
  </div>
</nav>
