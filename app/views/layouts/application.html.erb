<!DOCTYPE html>
<html>
  <head>
    <title>ShermanEval</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container w-full max-w-full">
      <% if current_user %><%#%>
        <%= render 'layouts/authenticated_header', user: current_user %><%#%>
      <% end %><%#%>
      <div class="flex container w-full max-w-full content-evenly mt-5">
        <% if notice.present? %><%#%>
          <p class='py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block' style= "margin-left: auto;margin-right:  auto;" id='notice'>
            <%= notice %><%#%>
          </p>
        <% end %><%#%>
        <% if alert.present? %><%#%>
          <p class='py-2 px-3 bg-red-50 mb-5 text-red-500 font-medium rounded-lg inline-block' style= "margin-left: auto;margin-right:  auto;" id='alert'>
            <%= alert %><%#%>
          </p>
        <% end %><%#%>
        </div>
        <% if local_assigns[:errors] %><%#%>
          <%= render 'layouts/object_errors', errors: local_assigns[:errors] %><%#%>
        <% end %><%#%>
      <br>
      
    </div>
    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
